<p id="notice"><%= notice %></p>

<!--<h1>Offers</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripcion</th>
      <th>Imagen</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @offers.each do |offer| %>
      <tr>
        <td><%= offer.name %></td>
        <td><%= offer.description %></td>
        <td><%= offer.picture %></td>
        <td><%= offer.user %></td>
        <td><%= link_to 'Show', offer %></td>
        <td><%= link_to 'Edit', edit_offer_path(offer) %></td>
        <td><%= link_to 'Destroy', offer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Offer', new_offer_path %> -->

<div class="container-fluid">

  <%=render 'layouts/header'%>

  <div class="row container-fluid">


    <%=render 'layouts/basicInfo'%>

    <div class="col container-fluid card">


      <div class="container card-header">

        <nav class="navbar navbar-light bg-faded">

          <ul class="nav nav-tabs card-header-tabs">
            <li class="nav-item">
              <a class="nav-link" href="/frame/inicio">Inicio	</a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="/frame/agregarOferta">Agregar Oferta</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/frame/categorias">Categorias</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/frame/verOfertas?cate=0">Ofertas</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/frame/verDetallesOferta">Perfil</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/frame/historial">Transacciones</a>
            </li>
          </ul>

          <button class="navbar-toggler btn btn-secondary float-right" type="button" data-toggle="collapse" href="#collapse1"><span class="navbar-toggler-icon my-toggler"></span></button>
          <div class="navbar-collapse collapse" id="collapse1">
            <ul class="navbar-nav float-right">
              <li class="nav-item active">
                <a class="nav-link" href="#">Opcion 1</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Opcion 2</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Opcion 3</a>
              </li>
            </ul>
          </div>
        </nav>
      </div>

      <table>
        <thead>
        <tr>
          <th>Nombre</th>
          <th>Descripcion</th>
          <th>Imagen</th>
          <th colspan="3"></th>
        </tr>
        </thead>

        <tbody>
        <% @offers.each do |offer| %>
            <tr>
              <td><%= offer.name %></td>
              <td><%= offer.description %></td>
              <td><%= offer.picture %></td>
              <td><%= link_to 'Mostrar', offer %></td>
              <td><%= link_to 'Editar', edit_offer_path(offer) %></td>
              <td><%= link_to 'Eliminar', offer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
        <% end %>
        </tbody>
      </table>

      <br>


      </div>
